# AI source art lifecycle

- `Generated/`: local, disposable generations and provenance sidecars; ignored by Git.
- `Approved/`: explicitly reviewed source art and matching sidecars; shared through Git LFS.

Approval means the file is worth sharing and iterating on. It does not automatically mean the asset is final, legally cleared for every use, or ready for direct Unity import.
